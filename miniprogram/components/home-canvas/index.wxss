.canvas {
  position: relative;
  flex: 1;
  width: 100%;
  border-radius: 44rpx 44rpx 54rpx 54rpx;
  background:
    linear-gradient(180deg, rgba(241, 244, 245, 0.88) 0%, rgba(239, 243, 246, 0.96) 100%);
  overflow: hidden;
}

.canvas__wash {
  position: absolute;
  inset: 0;
  background:
    radial-gradient(circle at 50% 12%, rgba(255, 255, 255, 0.72) 0%, rgba(255, 255, 255, 0) 34%),
    linear-gradient(180deg, rgba(255, 255, 255, 0.18) 0%, rgba(255, 255, 255, 0) 36%);
}

.canvas__glow {
  position: absolute;
  border-radius: 50%;
  filter: blur(48rpx);
  opacity: 0.7;
}

.canvas__glow--primary {
  width: 420rpx;
  height: 420rpx;
  left: 50%;
  top: 45%;
  transform: translate(-50%, -50%);
  background: rgba(133, 169, 192, 0.16);
}

.canvas__glow--secondary {
  width: 520rpx;
  height: 260rpx;
  left: 50%;
  bottom: 120rpx;
  transform: translateX(-50%);
  background: rgba(255, 255, 255, 0.58);
}

.canvas__stage {
  position: relative;
  z-index: 1;
  min-height: 100%;
  padding: 36rpx 36rpx 56rpx;
  box-sizing: border-box;
  display: flex;
  align-items: center;
  justify-content: center;
}

.canvas__card {
  margin: 220rpx 24rpx 220rpx;
  padding: 58rpx 42rpx 48rpx;
  border-radius: 40rpx;
  background: rgba(255, 255, 255, 0.94);
  display: flex;
  flex-direction: column;
  align-items: center;
  box-shadow:
    0 38rpx 88rpx rgba(26, 28, 46, 0.08),
    0 8rpx 18rpx rgba(26, 28, 46, 0.04);
}

.canvas__icon-orb {
  position: relative;
  width: 124rpx;
  height: 124rpx;
  border-radius: 50%;
  margin-bottom: 46rpx;
  background:
    linear-gradient(180deg, rgba(255, 255, 255, 0.98) 0%, rgba(246, 247, 249, 0.98) 100%);
  box-shadow:
    inset 0 0 0 2rpx rgba(26, 28, 46, 0.04),
    0 10rpx 26rpx rgba(26, 28, 46, 0.08);
}

.canvas__icon {
  width: 50rpx;
  height: 50rpx;
}

.canvas__title {
  font-size: 64rpx;
  line-height: 1.08;
  text-align: center;
  color: #1a1c2e;
  font-weight: 650;
  letter-spacing: -0.04em;
}

.canvas__subtitle {
  margin-top: 24rpx;
  font-size: 28rpx;
  line-height: 1.7;
  text-align: center;
  color: rgba(26, 28, 46, 0.68);
}

.canvas__hint {
  margin-top: 18rpx;
  font-size: 20rpx;
  line-height: 1.5;
  text-align: center;
  color: rgba(26, 28, 46, 0.36);
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.canvas__chips {
  margin-top: 52rpx;
  display: flex;
  justify-content: center;
  flex-wrap: wrap;
  gap: 18rpx;
}

.canvas__chip {
  min-width: 184rpx;
  padding: 18rpx 28rpx;
  border-radius: 999rpx;
  background: #edf1f3;
  color: #1a1c2e;
  font-size: 22rpx;
  line-height: 1;
  text-align: center;
}
